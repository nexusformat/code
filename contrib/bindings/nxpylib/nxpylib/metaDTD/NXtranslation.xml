<?xml version="1.0" encoding="UTF-8"?>
<!--
URL:     http://www.neutron.anl.gov/nexus/xml/NXtranslation.xml
Editor:  NIAC
$Id$

This is the description for the general spatial location
of a component - it is used by the NXgeometry.xml class
-->
<NXtranslation name="{name of translation}">
    <NXgeometry name="{geometry}">{Link to other object if we are  relative , else absent}?</NXgeometry>
    <distances type="NX_FLOAT[numobj,numdist]" units="">{(x,y,z), (r,z) or (r).}{This field and the angle field describe the position of a component. The value of "numdist" and "numang" uniquely define whether the coordinates are cartesian (numdist=3,num_angle=0), cylindrical (numdist=2,numang=1), or spherical (numdist=1,numang=2). For absolute position, the origin is the scattering center (where a perfectly aligned sample would be) with the z-axis pointing downstream and the y-axis pointing gravitationally up. For a relative position the NXtranslation is taken into account before the NXorientation. The axes are right-handed and orthonormal.}?</distances>
    <angles type="NX_FLOAT[numobj,numang]" units="degree|radian">{(azimuthal_angle,polar_angle) or (azumiuthal_angle). See the documentation in "distances" field.}?</angles>
</NXtranslation>
